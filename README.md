Secure Student Portal system. 

Build on Stack: Node.js/Express + SQLite + JWT (httpOnly cookies) + Vanilla HTML/CSS/JS Security: bcrypt, Helmet, rate limiting, input validation, RBAC, parameterized queries, CSRF-safe cookie strategy.
